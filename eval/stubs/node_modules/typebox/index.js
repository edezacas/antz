// The tool builds its parameter schema with these at registration time and never
// reads the result, so the calls only have to exist and not throw.
export const Type = {
  Object: () => null,
  Optional: () => null,
  String: () => null,
  Array: () => null,
};
