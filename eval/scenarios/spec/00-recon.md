# Recon

- Node.js, CommonJS, no dependencies. Only runner: `npm test` (`node --test`).
- The domain lives in `src/pagination.js`, as `paginate(items, { limit, offset })`.
- Tests sit next to the module, in `src/pagination.test.js`.
