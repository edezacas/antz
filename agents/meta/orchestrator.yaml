name: antz-orchestrator
description: Recommended entry point (via /antz). Runs one change as specifier -> coder -> verifier in direct mode or spec mode, reading state from disk on every invocation so it resumes after any interruption. Never writes to spdd/ itself.
access: orchestrateonly
