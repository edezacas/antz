test_command=sh tests/renderinject_test.sh
id=renderinject-01 result=green reason=install.sh injects each marker line's file content into the Claude Code orchestrator body (every non-empty line prefixed with the marker's own three-space fence indent, blank lines left empty exactly as the embedded snippet rendered); rendered body byte-identical to the pre-change render (unmasked full-tree cmp vs the base-commit render confirmed, same 4.2.1 marker), no "# antz-include:" survives, and the body matches an independent reconstruction of the contract
id=renderinject-02 result=green reason=same injection for OpenCode at its own frontmatter (mode: primary, edit: deny, deny-by-default task allowlist pinned unchanged); all three script contents injected at the same fences with no marker surviving
id=renderinject-03 result=green reason=rendered bodies (both clients) still carry the temp-file + "sh <tempfile> ..." runtime contract wording verbatim (flow subcommand line, skills invocation, probe run by state with CHANGE_DIR at the change directory); installed inventory is exactly the pre-change 12 files (diff -r against the base render clean)
id=renderinject-04 result=green reason=include files resolve through the one fetch_file helper (local read or RAW_BASE fetch, one executable curl invocation); a missing script (each of the three tested) exits non-zero naming the file, with no orchestrator body written and no marker fallback anywhere under HOME
id=renderinject-05 result=green reason=all ten non-orchestrator renders byte-identical to the pre-change render (version marker masked for future-proofing), only orchestrator.prompt carries markers (3), a marker appended to coder.prompt renders verbatim (keying is behavioral), and --check's report and no-write behavior are unchanged
id=renderinject-06 result=green reason=header tracked-set sentence now names agents/prompts/, agents/meta/, AND install.sh; the stale two-item sentence is gone; every other header line byte-identical to the base's
id=renderinject-07 result=green reason=AGENTS.md and CLAUDE.md gotcha bullets state the scripts' source of truth (scripts/orchestration/antz-flow.sh, antz-probe.sh) and the install.sh injection into the rendered antz-orchestrator body for both clients, keep the runtime wording (sh <tempfile> ..., nothing installed standalone), drop the stale "embedded" sourcing, and state it identically in both files; docs-bump_test.sh's pin on the old sentence evolved with this change (its own convention)
